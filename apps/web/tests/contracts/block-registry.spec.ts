import { describe, test } from 'vitest';

describe('block registry contract', () => {
  test.todo('registers the initial block set: Hero, RichText, Image, ImageText, Gallery, WorksGrid, Quote, SectionHeader, SeriesGrid, Contact, Divider');
  test.todo('exposes a Zod schema and default data factory for every block type');
  test.todo('rejects unknown block types before publish');
  test.todo('supports future extension without changing renderer call sites');
});
