FROM node:22-alpine

WORKDIR /app

ENV NEXT_TELEMETRY_DISABLED=1
ENV PORT=3000
ENV HOSTNAME=0.0.0.0

RUN npm install -g pnpm@10.8.0

CMD ["sh", "-lc", "pnpm install && pnpm --filter web dev --hostname 0.0.0.0 --port 3000"]
